# GSC-Indexierungs-Fixes — 2026-06-12
**Quelle:** GSC → Seitenindexierung (Stand 05.06.26: 26 indexiert, 8 nicht indexiert, 5 Gründe)

## Analyse der 5 GSC-Gründe

| Grund | Seiten | Betroffene URLs | Status Fix |
|---|---|---|---|
| 403 blockiert | 2 | `/praxis/`, `/konservativ/` | ✅ 301 seit 29.05. live, **Validierung gestartet 12.06.** |
| Alternative Seite mit kanonischem Tag | 1 | `/index.html` | ✅ Neu: 301 `/index.html` → `/` |
| Gefunden – nicht indexiert | 3 | `stosswellentherapie`, `bandscheibenvorfall`, `revision` | ✅ Indexierung beantragt 12.06. |
| Duplikat – andere kanonische Seite | 1 | `/operative-chirurgie.html` (Legacy, 301 seit 29.05.) | ✅ **Validierung gestartet 12.06.** |
| Gecrawlt – nicht indexiert | 1 | `/skoliose/korsett-therapie.html` | ✅ Bereits indexiert (URL-Prüfung 12.06.: „URL ist auf Google") |

## Implementierte Server-Fixes (deployt 12.06.)

1. **`.htaccess`: 410 Gone für Alt-Upload-Reste** — `/backup_seo/` (komplette Site-Kopie!)
   und `/seo_fix_log.txt` lagen öffentlich auf Strato (Deploy-Skript lädt nur additiv hoch).
   Bewusst NICHT per robots.txt gesperrt, damit Google den 410 crawlen und deindexieren kann.
2. **`.htaccess`: 301 `/index.html` → `/`** — mit `THE_REQUEST`-Condition gegen DirectoryIndex-Schleife.
3. **`sitemap.xml`: `<lastmod>` für alle 24 URLs** — aus Git-Commit-Daten generiert.
4. **Sitemap neu eingereicht** in GSC (12.06.26).

## Verifiziert (curl, 12.06.)
- `backup_seo/`, `backup_seo/index.html`, `seo_fix_log.txt` → 410
- `/index.html` → 301 → `/`
- Sitemap live mit 24× lastmod

## Nachtrag 12:43 — Breadcrumb-Schema-Fix (GSC Navigationspfade)

GSC meldete „Feld ‚item' fehlt (in itemListElement)" für `/praxis/ueber-dr-mosafer.html`.
Ursache: mittleres Breadcrumb-Element (z.B. „Praxis", „Wirbelsäulenchirurgie") ohne `item`-URL —
die Verzeichnis-Ebenen haben keine eigenen Seiten. **12 Seiten** hatten dasselbe Muster.

**Fix:** Mittleres ListItem entfernt, auf 2-stufige Breadcrumbs umgestellt (Home → Seite),
Positionen neu nummeriert. Deployt, live verifiziert, GSC-Validierung gestartet (12.06.).

## Nächste Kontrolle (ca. 26.06.)
- [ ] GSC-Validierungen (403 + Duplikat) auf „Bestanden" prüfen
- [ ] Indexierungsstatus der 3 beantragten Seiten prüfen
- [ ] Wenn `backup_seo` aus Google raus: Reste per SFTP vom Server löschen (optional)
